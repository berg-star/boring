# 无聊研究所：核心代码教学

这份文档按当前项目的实际代码编写，适合已经接触过一点 C++、但还不熟悉网站开发的读者。目标是能顺着代码解释一次操作，并知道修改功能时应该找哪个文件。代码摘录省略的部分会注明；不要把教学片段直接覆盖到源文件里。

建议阅读顺序：先读第 1–4 节，理解网站如何工作；再结合源码阅读转盘和宠物；最后看构建、测试与修改方法。

## 1. 先分清：哪些代码在哪里运行

网站由一个 C++ 服务和一组浏览器页面组成，没有 Node.js 后端，也没有数据库。

```text
访客的浏览器                             Railway 服务器
HTML：页面结构                          C++ + Crow：接收 HTTP 请求
CSS：颜色、排版、动画                    static/：网页、样式和脚本文件
JavaScript：点击、绘图、保存记录  ← HTTP → data/：题库、卡片等 JSON 内容
localStorage：这个浏览器的个人记录
```

例如打开命运转盘：服务器把 HTML、CSS、JavaScript 发给浏览器，后续转盘抽取和动画主要由浏览器完成。服务器没有替每个访客持续计算旋转角度。

| 文件或目录 | 负责什么 | 推荐怎么读 |
|---|---|---|
| [src/main.cpp](src/main.cpp) | 服务启动、路由、文件读取、内容校验、接口 | 先看 `main()`，再按调用找辅助函数 |
| [static/index.html](static/index.html) | 首页结构和玩法入口 | 找链接与游戏卡片 |
| [static/main.js](static/main.js) | 首页筛选、随机跳转 | 从按钮的事件监听开始 |
| [static/style.css](static/style.css) | 公共样式和响应式布局 | 搜页面元素的 class |
| [static/random.js](static/random.js) | 奇怪问题的请求和显示 | 最简单的前后端联动例子 |
| [static/wheel.js](static/wheel.js) | 转盘状态、选项编辑、抽取、保存 | 本文的主要前端例子 |
| [static/wheel-presets.js](static/wheel-presets.js) | 九类场景和品牌饮品预设 | 改默认菜单从这里入手 |
| `data/` | 服务端内置内容 | 是普通文件，不是用户数据库 |
| [CMakeLists.txt](CMakeLists.txt) | 下载依赖、编译、复制资源 | 理解源码如何变成程序 |
| [Dockerfile](Dockerfile) | 在 Linux 中编译并装配运行环境 | 理解线上环境 |
| `tools/` | 自动检查脚本及开发依赖 | 不参与正常网站运行 |

本项目是多个 HTML 页面组成的网站。打开另一个玩法通常会导航到另一个页面，并重新执行该页脚本。

## 2. 跟着一次“奇怪问题”请求读代码

### 2.1 HTML 先放好位置，JavaScript 再填内容

HTML 中的 `id` 相当于给元素起一个便于查找的名字。`random.js` 通过下面的函数填文字：

```js
function text(id, value) {
  document.getElementById(id).textContent = value;
}
```

`document` 代表当前网页；`getElementById` 找到元素；`textContent` 修改纯文字。用户输入或题目内容使用 `textContent`，不会被当成 HTML 标签执行。

脚本最后执行：

```js
action.addEventListener("click", draw);
draw();
```

第一行注册点击处理函数，没有立刻调用 `draw`；第二行带括号，是页面加载时主动调用一次。因此首次进入有内容，之后可以点击按钮重新抽取。

### 2.2 浏览器发出请求

`draw()` 中的核心代码如下，省略了按钮状态和错误提示：

```js
const response = await fetch(endpoints[mode], {
  cache: "no-store",
  signal: controller.signal
});
if (!response.ok) throw new Error("http-error");
const data = await response.json();
validate(data);
text("question", data.question);
```

- `endpoints[mode]` 在这个脚本里对应 `/api/random-question`。
- `fetch` 发送 HTTP 请求；这里默认使用 GET。
- `await` 等待异步结果，不会让整个浏览器停止响应。
- `response.ok` 检查 HTTP 状态是否成功。返回 404、500 时，不能直接当成正常题目处理。
- `response.json()` 把 JSON 响应转换成 JavaScript 对象。
- `validate` 检查对象确实有非空的 `question` 字符串。

源码用 `AbortController` 配合 8 秒定时器取消超时请求；用 `busy` 和禁用按钮避免重复点击；在 `finally` 中恢复按钮。无论成功还是失败，用户都应能继续操作。

### 2.3 C++ 收到请求，选一条题目

在 `main.cpp` 中：

```cpp
CROW_ROUTE(app, "/api/random-question")([&] {
    return random_record(questions);
});
```

`CROW_ROUTE` 注册路由，也就是“哪个网址交给哪段代码处理”。它不是启动时立刻抽一道题，而是注册一个以后收到请求才会调用的函数。

`[&] { ... }` 是 C++ lambda。这里按引用使用 `main()` 中的 `questions`，避免复制整份题库。`app.run()` 持续运行期间，`main()` 的这些局部数据仍然存在。

`random_record()` 的核心是：

```cpp
thread_local std::mt19937 generator(std::random_device{}());
std::uniform_int_distribution<std::size_t> choose(0, records.size() - 1);
crow::response response(records[choose(generator)]);
```

- `mt19937` 是伪随机数引擎，用于娱乐抽取。
- `random_device` 为引擎提供初始种子。
- `thread_local` 让每个处理线程拥有自己的引擎，避免多个线程同时修改同一个引擎。
- `uniform_int_distribution` 在包含两个端点的整数区间内等概率选下标。
- 内容数组不为空，是启动阶段 `load_records()` 已经检查的前提。

结果被设置为 `application/json; charset=utf-8` 并返回。浏览器收到后，把其中的 `question` 放进页面。

完整过程就是：**点击 → fetch → Crow 路由 → 随机选题 → 返回 JSON → 修改页面文字**。

## 3. C++ 后端的几个核心设计

### 3.1 启动时准备内容，请求时直接使用

`main()` 先调用 `load_records()` 读取 JSON 文件：

```cpp
const auto questions = load_records(root / "data/questions.json", {"question"});
```

这里 `root / "data/questions.json"` 是 `std::filesystem::path` 的路径拼接，不是数字除法。

`load_records()` 会检查：顶层是不是非空数组；每条是不是对象；指定文字字段是否存在且非空；指定数值字段是否在 0–100 范围内。它把每条内容序列化为字符串，保存在 `vector<string>` 中。

因此题库请求不需要每次重新读取磁盘。但修改 `data/` 后，需要让运行中的服务重新加载，也就是重启或重新部署；只改文件不会自动更新内存中的题库。

卡片还有额外的 ID、系列和稀有度检查，真心话还会检查两个类别。通用校验并不代表所有业务限制都在同一个函数里。

### 3.2 静态文件与接口有不同职责

| 路径 | 后端做什么 |
|---|---|
| `/` | 返回首页 HTML |
| `/wheel.html` 等 | 检查页面白名单，再返回 HTML |
| `/static/wheel.js` 等 | 检查资源白名单，再返回文件 |
| `/api/random-question` | 随机返回一道问题 |
| `/api/random-card` | 随机返回一张卡片 |
| `/api/random-fun` | 随机返回一份整活内容 |
| `/api/cards` | 返回整套卡片，供收藏册展示 |
| `/api/truth-questions` | 返回完整真心话题库 |
| `/api/book-answers` | 返回完整答案库 |
| `/healthz` | 返回服务健康状态 |
| `/api/hello` | 简单的 C++ 连通性示例 |

`serve_file()` 读取文件，并根据扩展名设置 `Content-Type`，告诉浏览器这是 HTML、CSS 还是 JavaScript。

`pages` 与 `assets` 是允许公开访问的文件集合。新增文件后，如果忘记把它加入对应集合，就可能出现文件存在但网页请求返回 404 的情况。源码还关闭了 Crow 默认静态目录功能，公开范围由这些路由控制。

### 3.3 HOST、PORT 与资源路径

- 默认 `HOST=127.0.0.1`：只监听本机。
- 容器使用 `HOST=0.0.0.0`：接受容器网络接口上的请求，便于托管平台转发。
- 默认 `PORT=18080`；线上可以用环境变量指定端口。
- `server_port()` 校验端口是 1–65535 的整数，避免错误配置被默默接受。
- `executable_directory()` 根据程序自身的位置寻找 `static/`、`data/`，而非依赖终端当前目录；也允许通过第一个命令行参数指定资源目录。

`0.0.0.0` 是监听地址，不是访客应该输入的网址。浏览器访问的是 Railway 提供的域名。

`app.bindaddr(host).port(port).concurrency(2).run()` 设置监听地址、端口和两个工作线程，然后持续处理请求。

### 3.4 为什么刷新后可能仍是旧版

文件响应设置 `Cache-Control: no-cache`：允许缓存，但再次使用前需要向服务器确认。它和“不保存响应”的 `no-store` 不一样。

即使浏览器重新请求了，如果 Railway 还没完成新版本部署，服务器也只能返回旧文件。**git push 完成、构建通过、部署成功、公网已经返回新版，是不同阶段。**

## 4. 各个玩法到底在哪里计算

所有玩法的网页文件都由 C++ 发给浏览器，但并非所有玩法逻辑都由 C++ 计算。

| 玩法 | C++ 的额外职责 | 浏览器 JavaScript 的职责 |
|---|---|---|
| 反应测试 | 无专用玩法接口 | 等待变色、计算毫秒数、记录本次会话最佳成绩 |
| 命运转盘 | 无专用玩法接口 | 分类、编辑选项、随机结果、动画、存档 |
| 今日抽卡 | 随机卡片接口、完整卡片接口 | 翻卡、收藏、卡册筛选、生成分享图片 |
| 奇怪问题 | 随机问题接口 | 请求与显示 |
| 随机整活 | 随机内容接口 | 分类型展示、分享操作 |
| 真心话 | 返回完整题库 | 分类、洗牌、抽题、跳过 |
| 养小东西 | 无专用玩法接口 | 外观、互动、时间变化、本地存档 |
| 无聊星球 | 无专用玩法接口 | 绘制、拖动旋转、事件、合成音效 |
| 答案之书 | 返回完整答案库 | 洗牌、翻页、复制、合成翻页声 |
| 成就系统 | 无专用玩法接口 | 接收玩法事件、检查条件、保存与提示 |

## 5. 拆解命运转盘：状态、画面与随机结果

对照 [wheel.js](static/wheel.js) 和 [wheel-presets.js](static/wheel-presets.js) 阅读。

### 5.1 数据和逻辑分开

`WheelPresets` 保存默认内容，`wheel.js` 负责如何使用它们。两者通过 `window.WheelPresets` 连接，HTML 中预设脚本在玩法脚本之前加载。

下面是默认数据的简化形式：

```js
food: { name: "🍜 午饭吃什么", items: ["盖浇饭", "拌面", "饺子"] }
```

如果只想修改默认午饭，一般改预设即可。但已有浏览器保存过自己的午饭列表时，会优先继续使用存档；需要点击“恢复当前默认”才会采用新预设。

### 5.2 state 是当前玩法的记录

初始结构：

```js
let state = { scene: "food", brand: "choose", custom: [], lists: {} };
```

`scene` 是场景，`brand` 是饮品模式或店铺，`custom` 是自定义店铺，`lists` 保存各组独立菜单。

`scope()` 为当前菜单找一个键：午饭用 `food`，蜜雪冰城用 `drink:mixue`，先抽店铺用 `shops`。由于保存位置不同，修改午饭不会覆盖奶茶列表。

列表里的每项是 `{text, on}`：`text` 为文字，`on` 表示是否参与抽取。取消勾选保留了文字，但抽取前的 `filter(x => x.on)` 会排除它。

`render()` 根据状态更新选项列表与界面，`draw()` 绘制圆盘，`save()` 保存状态。`reset()` 会重置本轮结果和角度，再更新界面并保存；它不等于恢复所有默认菜单。

### 5.3 先决定抽中谁，再让转盘停在那里

核心选择：

```js
const items = list().filter(x => x.on);
const index = Math.floor(Math.random() * items.length);
const chosen = items[index].text;
```

`Math.random()` 给出大于等于 0、小于 1 的值。假设有 6 个选项，乘 6 后向下取整，就得到 0–5 的下标。没有给某家店额外加权。

接着计算角度，下面将源码中的表达式拆开方便理解：

```js
const segment = 360 / items.length;
const target = (360 - (index + 0.5) * segment) % 360;
rotation += 1800 + (target - rotation % 360 + 360) % 360;
```

- `segment` 是每个扇区的角度。
- `index + 0.5` 取扇区中心，避免停在分界线上。
- `1800` 是额外转五圈，用于动画效果。
- 最后一段求当前角度到目标方向的补转角度。

画圆盘时已经把起始角放在顶部，目标角度与这个绘图约定配合，使选中的扇区中心停在固定指针下。不是先转一圈再观察指针碰巧指向谁。

### 5.4 为什么需要 spinning 和两个结束机制

转动期间 `spinning=true`，编辑和抽取按钮被禁用。否则动画开始时有 6 个选项，中途删成 5 个，指针与结果就可能不对应。

动画正常完成会触发 `transitionend`；源码同时设了超时兜底，避免事件未触发时按钮永远不能用。`finish()` 开头检查状态，确保完成处理和成就计数不会执行两次。

系统设置了“减少动态效果”时，动画时长设为 0，抽取结果逻辑仍然正常。

## 6. 本地存档与宠物的时间变化

### 6.1 localStorage 不是服务器数据库

保存和读取对象的基本方式：

```js
localStorage.setItem(KEY, JSON.stringify(state));
const saved = JSON.parse(localStorage.getItem(KEY));
```

这只是语法示意，实际读取前还要判断是否存在、捕获异常并校验字段，不能默认旧记录永远正确。

`localStorage` 保存字符串，因此对象要先转换成 JSON。记录按网站来源隔离：协议、域名、端口不同，通常就不是同一份存储。所以本地网站与 Railway 网站的宠物不共用，换浏览器或设备也不会自动同步。

清除“缓存图片和文件”与清除“网站数据”不是同一件事；真正删除本地存储的网站数据才会清掉这些记录。

| 记录 | 存储键或位置 |
|---|---|
| 转盘菜单 | `boring-lab-wheel-v1` |
| 宠物 | `boring-lab-pet-v1` |
| 卡片收集与收藏 | `boring-lab-cards-v1` |
| 成就 | `boring-lab-achievements-v1` |
| 反应测试最佳成绩 | `sessionStorage` 中的 `boring-lab-best` |

`sessionStorage` 通常用于当前标签页会话，刷新可以保留；不要把它当成长期存档。

### 6.2 页面关了，宠物怎样“继续长大”

[pet.js](static/pet.js) 的 `advance()` 用时间差计算变化：

```js
const elapsed = Math.max(0, now - state.updated);
const hours = Math.min(elapsed / 3600000, 168);
state.food = clamp(state.food - hours * 3);
```

上次存档保存了 `updated`。下次打开时，用当前时间减去它，换算成小时，再扣除饱食度。计算最多计入 168 小时，`clamp()` 则把数值限制在 0–100。

因此不需要服务器在用户离开后每秒喂养或更新宠物。这里的变化是重新打开时按时间差补算，页面内互动时也会先推进时间。浏览器本地时间可被用户调整，所以它不是防作弊机制。

`render()` 把体重转换成 CSS 宽度，把颜色放到 CSS 自定义属性里；鼠标移动时调整眼睛偏移。数据决定外观，CSS 负责显示。

## 7. 另外几个值得学习的实现

### 7.1 成就是一个共用的事件接收器

玩法完成后调用：

```js
window.BoringAchievements?.record("wheel");
```

`?.` 是可选链：如果成就对象不存在，就不调用它，避免影响正常游戏。

[achievements.js](static/achievements.js) 把 `wheel` 转换成旋转次数，再检查条件。例如 `spins >= 10` 解锁“命运外包专家”。解锁记录中保存了日期，已经解锁的不会重复弹出。

这样各玩法只需要报告“发生了什么”，具体成就条件集中在一处维护。对支持 Web Locks 的浏览器，存储更新会申请锁，降低多个标签页同时写入时互相覆盖的风险；不支持时走普通更新分支。

### 7.2 反应测试用状态区分同一个点击

[reaction.js](static/reaction.js) 中，点击的含义取决于 `state`：

```text
idle/result/early → 点击开始 → waiting
waiting → 定时等待结束 → ready
waiting → 提前点击 → early
ready → 点击 → result
```

计时使用 `performance.now()`，计算当前时间与变绿起点之差；随机等待为 1500–5000 毫秒。切换页面会取消正在等待或计时的这一轮，避免后台标签页影响结果。

### 7.3 答案之书和真心话是“洗牌后逐个取”

[book.js](static/book.js) 的 `take()`、[truth.js](static/truth.js) 的 `next()` 都维护剩余内容。用完后重新洗牌，再通过 `pop()` 逐个取出，并避免新一轮第一条刚好接上上一轮最后一条。

这与随机问题接口的“每次独立抽一条”不同：独立抽取可以很快重复；洗牌在一轮内不重复。刷新页面后，这些内存中的牌堆会重新建立。

### 7.4 星球不是一个需要下载的大型视频

[planet.js](static/planet.js) 用 Canvas 绘图。`rotate()` 调整空间坐标，`project()` 把它映射到画布，拖动修改旋转角度后重新绘制。

音效使用 Web Audio API 的音调、噪声和滤波器合成，没有依赖一整套 MP3 文件。`requestAnimationFrame` 用于需要动画的绘制；页面隐藏时会限制动画或停止声音，减少无意义的工作。

### 7.5 卡片数量不能只改一个 JSON

[cards.js](static/cards.js) 当前按 36 张卡片设计：前端校验总数，ID 限制为六个系列各 `01`–`06`，收藏计数与部分文案也使用 36。

因此“给卡片追加第 37 张”不是只往 JSON 末尾加一条。还要同步修改前端校验、数量上限、文案和相关测试。阅读代码时要特别注意这种写死的业务限制。

## 8. 构建、运行、部署各做了什么

[CMakeLists.txt](CMakeLists.txt) 指定 C++17，获取固定版本的 Crow 和 Asio，再构建 `boring_lab`。`target_link_libraries` 让目标使用 Crow 提供的依赖配置；Windows 还需要系统网络库。

`copy_resources` 会把源码目录里的 `static/` 与 `data/` 复制到程序所在目录。**日常应修改根目录里的源码，不要只改 `build/Release/static/`**，因为后者是复制结果，下次构建可能被覆盖。

在已经完成 Windows 配置的项目中：

```powershell
cmake --build build --config Release --parallel 2
.uild\Releaseoring_lab.exe
```

第一条编译并复制资源；第二条启动服务。如果旧程序正在运行，修改 C++ 后重新编译前应先停止它，避免可执行文件被占用。首次配置与安装步骤见 README。

[Dockerfile](Dockerfile) 分两阶段：第一阶段带编译器，在 Linux 中生成程序；第二阶段只保留运行库、程序和资源，并使用普通用户运行。线上 Linux 程序不是把 Windows 的 `.exe` 原样搬过去。

Git 的 commit 是代码快照，push 是上传快照。部署平台还要构建和启动程序；GitHub 的 Docker 检查通过，也不等于 Railway 已切换公网流量。

## 9. 以后修改功能，应该从哪里动手

| 想做的修改 | 优先查看 |
|---|---|
| 改默认午饭、饮品 | `static/wheel-presets.js`，然后考虑已有存档 |
| 改转盘动画、限制或排除行为 | `static/wheel.js` |
| 增加问题、答案 | 对应 `data/*.json` 与前后端校验 |
| 改宠物喂养数值 | `static/pet.js` 的 `interact()`、`advance()` |
| 改公共颜色、间距 | `static/style.css`；仅单个玩法则优先专用 CSS |
| 增加成就 | `achievements.js` 的目录、状态、校验和计数展示，并接入玩法事件 |
| 增加新页面 | HTML/JS/CSS、后端白名单、首页入口、随机跳转列表及相关检查 |
| 做跨设备存档或公共排行榜 | 需要新的后端保存接口和持久化设计，不能只靠 localStorage |

建议每次只做一个可解释的变化：改代码 → 本地验证 → 检查差异 → commit → 按需要推送部署 → 确认公网内容。

现有验收工具可以复用，例如：

```powershell
python tools/check-http.py http://127.0.0.1:18080
node tools/check-wheel.cjs
```

第一条检查 HTTP 页面、资源和接口；第二条用浏览器操作转盘。运行前需要本地服务可访问，浏览器检查还需要 `tools` 中已安装测试依赖及 Chrome。Node.js 在这里是测试工具，不是网站后端。

## 10. 读完后可以用这几个问题自测

1. 为什么清除浏览器网站数据会丢失宠物，却不会删除服务器题库？
2. 为什么改变午饭菜单不会影响蜜雪冰城菜单？找到 `scope()` 的对应分支。
3. 为什么按钮禁用之后，还要在函数里检查 `busy` 或 `spinning`？
4. 为什么转盘的指针与结果一致，需要随机下标、绘图起点、目标角度一起配合？
5. 为什么改了 `data/questions.json` 后，需要重启服务？
6. 为什么本地已经改好图标，公网仍可能显示旧版？

能沿着源码说明这些问题，就已经理解了这个项目最主要的运行方式。接下来可以先改一个默认选项或一条题目，观察数据如何进入界面，再逐渐修改交互逻辑。
